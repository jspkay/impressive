// The class TransitionManager is not essential for the whole thing 
// to work, nevertheless it adds pleasure to the eye during the 
// presentation, in that makes the transitions smooth.
// This compontent determines the transition between two steps. 
// If a transition is not determined for the present steps, then 
// it simply will correspond to css property transition="",
// which means that there is gonna be just a simple jump
export class TransitionManager{
  constructor(){
    var transitions = [];
  }
}
